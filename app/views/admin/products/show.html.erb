<div class='container'>
     <div calss="row align-items-start">
         <h3 class="text-center", style="background-color:#eee; width:120px; margin:10px 0 30px 100px;"><b>商品説明</b></h3>
    </div>
    <div class="form-group row justify-content-center">
        <div class="col-6">
            <%= image_tag @product.get_image_id, size: '220x150' %>
        </div>
            <p class="col-2">商品名</p>
            <p class="col-4"><%= @product.name %></p>
            <p class="col-2">商品説明</p>
            <p class="col-4"><%= @product.introduction %></p>
            <p class="col-2">ジャンル</p>
            <p class="col-4"><%= @product.genre.name %></p>
            <p class="col-2">税抜価格(税込価格）</p>
            <p class="col-4"><%= @product.price %>(<%= @product.add_tax_price %>)円</p>
            <% if @product.is_active? %>
              <p class="text-success">販売中</p>
            <% else %>
              <p class="text-black-50">販売停止中</p>
          <% end %>
          </div>
        <%= link_to "編集", edit_admin_product_path(@product), class: 'btn btn-success mx-auto' %>
    </div>
</div>