<%= flash[:notice] %>
<div class='container'>
    <div calss="row align-items-start">
        <h3 class="text-center", style="background-color:#eee; width:120px; margin:10px 0 30px 100px;"><b>商品編集</b></h3>
    </div>
        <%= form_with model: @product, url: admin_product_path,method: :patch, local:true do |f| %>
            <%= render 'form', product: @product, genres: @genres, f: f %>
            <div class="row">
                <%= f.submit "登録", class: 'btn btn-success mx-auto rounded-pill' %>
            </div>
        <% end %>
</div>
