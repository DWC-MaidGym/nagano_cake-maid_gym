<h2>ジャンル一覧・追加</h2>

<%= form_with model: @genre_new, local: true do |f| %>
  ジャンル名
  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: 'form-control book_title' %>
  </div>
  <div class="form-group">
    <%= f.submit class: 'btn btn-success' %>
  </div>
<% end %>